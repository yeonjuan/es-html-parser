{
  "name": "es-html-parser",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "format": "prettier --write .",
    "build": "tsc -b tsconfig.build.json",
    "ts": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.4"
  }
}
